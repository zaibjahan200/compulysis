apiVersion: v1
kind: Service
metadata:
  name: compulysis-frontend-service
  labels:
    app: compulysis-frontend
spec:
  type: NodePort
  selector:
    app: compulysis-frontend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30800
