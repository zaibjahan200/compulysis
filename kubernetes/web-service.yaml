apiVersion: v1
kind: Service
metadata:
  name: compulysis-web-service
  labels:
    app: compulysis-web
spec:
  type: NodePort
  selector:
    app: compulysis-web
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080
